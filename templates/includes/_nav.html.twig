<nav class="sb-topnav navbar navbar-expand navbar-dark bg-dark">
    <a class="navbar-brand ps-3" href="{{ path("app_default_home") }}">{{ app.user.game.name }}</a>
    <button class="btn btn-link btn-sm order-1 order-lg-0 me-4 me-lg-0" id="sidebarToggle" href="#!"><i class="fas fa-bars"></i></button>
    <form class="d-none d-md-inline-block form-inline ms-auto me-0 me-md-3 my-2 my-md-0">
    </form>
    <!-- Navbar-->
    <ul class="navbar-nav ms-auto ms-md-0 me-3 me-lg-4">
        <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" id="navbarDropdown" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false"><i class="fas fa-user fa-fw"></i></a>
            <ul class="dropdown-menu dropdown-menu-end bg-dark" aria-labelledby="navbarDropdown">
                {% if is_granted('ROLE_MJ') %}
                    <li><a class="dropdown-item text-light" onclick="return confirm('Voulez-vous vraiment supprimer la partie ?')" href="{{ path('app_game_delete', {'game': app.user.game.id}) }}">Supprimer la partie</a></li>
                    <li><hr class="dropdown-divider" /></li>
                {% endif %}
                <li><a class="dropdown-item text-light" href="{{ path('app_player_logout') }}">Déconnexion</a></li>
            </ul>
        </li>
    </ul>
</nav>